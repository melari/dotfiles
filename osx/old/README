Installation
============

$ ./install

You have to log out and back in to pick up the shell changes.

On Mac:
  - If using iterm2 make sure to change preferences > profiles > Terminal > Report Terminal Type to "xterm-256color"
