# Laptop power button behaviour.
#
# systemd's default for HandlePowerKey is `poweroff`, which shuts the machine
# down on a stray tap. Sleep instead, and keep a hard power-off available by
# holding the button (the long-press default is `ignore`).

[Login]
HandlePowerKey=suspend
HandlePowerKeyLongPress=poweroff
